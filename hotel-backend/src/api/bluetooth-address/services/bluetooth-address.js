'use strict';

/**
 * bluetooth-address service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bluetooth-address.bluetooth-address');
